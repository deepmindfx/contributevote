
// This is a read-only file, but we need to fix the type error
// Since we can't modify the actual file, we'll create a separate wrapper
// component that handles the type issue

// Note: This is a workaround since we can't modify the original file
// We'll need to let the user know about this limitation
